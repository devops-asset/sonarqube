dist: trusty
sudo: required

addons:
  sonarcloud:
    organization: "devops-asset"
    token: $SONAR_TOKEN1

jdk:
  - oraclejdk8

script:
  - mvn sonar:sonar 
  
cache:
  directories:
    - '$HOME/.sonar/cache'
